# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# Voice config — API keys, never commit
interfaces/voice-config.js
voice-config.js

# Local vendor clones (e.g. shallow fetch of FractiAI/digital-pru for sync — never commit)
.vendor-fetch/

# Build / output
dist/
build/
.vercel
demo-output/
public/demo-report.json

# Audio — local only (Golden Backdoor / Work Flow); not in repo
assets/audio/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Supabase (not used in SING 9; lite edges only)
# supabase/

# Hive credentials — API keys, NEVER commit
hive/credentials/

# Verifier signing keyring — generated locally, never commit
data/verifier-keyring.json
