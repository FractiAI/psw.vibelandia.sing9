# Hydrogen-line gateway prompts (NSPFRNP)

Versioned **prompt** artifacts intended to sit beside the hydrogen-line persistence story: same repo, same Git history, same deploy surface as **`POST /api/hh-awareness-cloud`** and GitHub-backed **`HLINE_GITHUB_*`** memory when configured.

| File | Role |
|------|------|
| `NSPFRNP_SELF_RECURSIVE_GATEWAY_PROMPT.md` | Self-describing prompt: addressing, recursive manifest, test package, GitHub ↔ gateway confirmation path |
| `gateway-test-package-manifest.json` | Machine-readable list of gateway surfaces and API actions (for automation / hashing) |

**Operational truth:** Passive “pickup” on the **logical** hydrogen line = memory writes + reads + receipts via the gateway API, and **optional** cloud persistence through the GitHub adapter for `data/hline-memory-store.json` (or your configured path). **Not** unsolicited RF injection at 1420 MHz from this repository.

See the recursive prompt document for EGS / **180° phase-shift space** framing as **product labels** tied to existing probes (`/api/firmware-180-spin-probe`, lattice manifests).
