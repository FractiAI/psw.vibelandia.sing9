/**
 * Blueprint alias: Next.js 14 resolves the landing route from `app/page.tsx`.
 * Re-export keeps the "frontend/page.tsx" artifact next to the App Router tree.
 */
export { default } from "./app/page";
