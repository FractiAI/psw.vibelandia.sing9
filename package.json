{
  "name": "psw-vibelandia-sing9",
  "private": true,
  "description": "SING 9 edge — intent tests define singularity contracts (lib/houdini-singularity.mjs).",
  "scripts": {
    "test": "node --test tests/intent/*.test.mjs",
    "test:intent": "node --test tests/intent/*.test.mjs",
    "build:static": "node scripts/vercel-static-output.mjs",
    "dev": "npm run build:static && vercel dev",
    "dev:vercel": "vercel dev",
    "preview:dist": "npm run build:static && npx --yes serve@14 dist -p 4173"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "vercel": "^41.4.1"
  }
}
