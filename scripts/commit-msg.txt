fix: solver.yml — remove cache: npm, no package-lock.json at root

cache: 'npm' in setup-node@v4 requires a package-lock.json at the repo
root. This repo has no root package.json — solver runs plain Node scripts
with no npm install step. Removing the cache directive unblocks the workflow.
