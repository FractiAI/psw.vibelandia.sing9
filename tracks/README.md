# `/tracks` — Hero Jo Hit Factory exports

Snap writes **musical metadata scores** here (JSON) after a successful compile.  
Lyrics-first; DAW integration is downstream of the OFC JSON schema in `/_EXPORT_SCHEMA.json`.
