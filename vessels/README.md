# `/vessels` — Snap templates (VALETPRU-ASIC shaped)

Templates target **silicon**, **SDR**, and **bio-platform** postures without pinning a single vendor stack.

See `silicon-sdr-bio.template.yaml` for a minimal stub.
